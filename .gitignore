.next/
node_modules/
out/
dist/
*.tsbuildinfo
.DS_Store
public/fonts/
.claude/
temp_auto_push.bat
temp_interactive_push.bat
config.bat

# Local environment / secrets — never commit
.env*.local

# Playwright MCP artifacts (screenshots, console/page dumps)
.playwright-mcp/
pic-live-full.jpeg

# Local reference material (design PDFs, screen captures) — kept on disk, not in git
screencapture-*.pdf
next-panic-*.log
